tags:
  - Room
summary: Get all rooms
description: Retrieve a list of all rooms
produces:
  - application/json

responses:
  200:
    description: List of rooms
    schema:
      type: array
      items:
        type: object
        properties:
          id:
            type: string
          cinema_id:
            type: string
          name:
            type: string
          total_seats:
            type: integer
