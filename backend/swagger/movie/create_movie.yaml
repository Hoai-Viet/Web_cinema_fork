tags:
  - Movies
summary: Tạo phim mới
requestBody:
  required: true
  content:
    application/json:
      schema:
        $ref: "#/components/schemas/MovieCreateRequest"
responses:
  201:
    description: Tạo thành công
  400:
    description: Thiếu dữ liệu hoặc tên phim đã tồn tại
